public class ArithmeticOperation<K, V> implements CodeFundamentals {
  
  private Operator operator;
  private State left;
  private State right;
  
  public enum Operator { Add, Sub, Mult, Div, Rem; }
  
  public ArithmeticOperation(Operator operator, State left , State right) {
    this.operator = operator;
    this.left = left;
    this.right = right;
  }
  
  
  public Integer value(State s) {
    
  }
    